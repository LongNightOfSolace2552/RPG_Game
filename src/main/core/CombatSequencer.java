package main.core;

// Runs a resolved battle sequence on a background thread, feeding each BattleAction's dialogue to the CombatRenderer one at a time with a short delay between lines, so dialogue displays progressively rather than all at once.
public class CombatSequencer {

}
